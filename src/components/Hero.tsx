import Menu from "./Menu";

const Hero = () => {
  return (
    <section>
      <div className="container max-w-6xl mx-auto px-6 md:px-0">
        <Menu />
      </div>
    </section>
  );
};

export default Hero;
